{
  "name": "orderflow-email-api",
  "version": "1.0.0",
  "description": "Standalone HTTP API for Ausmet email order extraction.",
  "license": "MIT",
  "type": "module",
  "main": "dist/server/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server/main.js",
    "test": "npm run typecheck",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "imapflow": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24"
  }
}
